name: Demo app tests

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v1
        with:
          node-version: 12
      - name: Install dependencies
        run: yarn
      - name: Run unit tests
        run: yarn test:unit
      - name: Start app
        run: yarn start &
      - name: Run e2e tests
        run: yarn test:e2e
